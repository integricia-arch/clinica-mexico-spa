# Environment variables — NEVER commit secrets
.env
.env.*
!.env.example
integrika_sync.config.ps1
client_secret*.json

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
manual-site/build
manual-site/.docusaurus

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local Claude Code config
.claude/settings.local.json

# Supabase CLI temp files
supabase/.temp/

# Audit artifacts
AUDITORIA_*.txt
auditar_*.ps1

# Temp de debugging (Claude/build)
_build_*.txt
_build_*.cmd
dist_integrika.zip

# Config local de herramientas
# .claude/ se ignora completo SALVO las skills del proyecto (.claude/skills/),
# que sí se versionan. project-context.md (credenciales) sigue ignorado.
.claude/*
!.claude/skills/
.claude/skills/*
!.claude/skills/fixer/
.agents/
skills-lock.json

# Obsidian vault config (local only) — memoria/ SÍ se versiona (ver CLAUDE.md)
.obsidian/

# Git worktrees locales (subagent-driven-development)
.worktrees/

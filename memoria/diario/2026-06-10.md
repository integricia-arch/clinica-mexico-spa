---
tags: [diario, farmacia, caja, responsive]
fecha: 2026-06-10
proyecto: "[[clinica-mexico-spa]]"
---

# Sesión 2026-06-10

## Resumen
Dos bloques: (1) responsive design del POS/sidebar, (2) diagnóstico y fix completo del ciclo de pharmacy shifts y persistencia de IVA.

## Completado hoy

### Responsive Design (sesión matutina)
- `useSidebarState` hook nuevo — centraliza estado open/collapsed/tablet
- `AppLayout.tsx` refactorizado con hook, breakpoints lg→xl, desktop collapse con iconos
- `PuntoDeVenta.tsx`: grid xl:3-col / md:2-col, sidebar izquierdo oculto bajo xl
- `Frecuentes` como acordeón en tablet, sticky cobro, touch targets, stock badges
- `PaymentCapture.tsx`: altura responsiva
- `Facturacion.tsx`: columnas Concepto y CFDI lg→xl
- Spec: `docs/superpowers/specs/2026-06-10-responsive-*`
- Commits: `5080680`, `48035ba`, `687c4f4`

### Corrección de IVA en DB
**Problema**: `pharmacy_register_sale` nunca guardaba `total_iva`, `base_imponible`, `iva_amount`.
**Fix**: Calcular por item desde `medicamentos.tasa_iva`. Precios incluyen IVA → `base = round(subtotal/(1+tasa_iva), 2)`.
Archivo: `supabase/migrations/_tmp_fix_pharmacy_iva.sql`
Commit: `84e724b`

### Fix ciclo pharmacy shift (raíz del `pharmacy_shift_id = NULL`)
**Diagnóstico**:
1. `pharmacy_open_shift` rechazaba con excepción si ya había shift abierto
2. `turno_close` nunca cerraba `pharmacy_cash_shifts` → shifts estancados
3. Cajero `26a7bacd`: shift `d484c4aa` abierto Jun 9, nunca cerrado → todos los turnos Jun 9-10 tenían `pharmacy_shift_id=NULL`

**Fixes**:
- `pharmacy_open_shift`: idempotente (retorna shift existente si ya hay uno abierto); agrega rol `cajero` al check de permisos
- `turno_close`: cierra el `pharmacy_cash_shift` vinculado (o encontrado via fallback) al cerrar turno
- One-time: cerró shift estancado `d484c4aa` manualmente
- Overload huérfano `turno_close(uuid, numeric, text)` eliminado

Archivo: `supabase/migrations/_tmp_fix_pharmacy_shift_lifecycle.sql`
Commit: `4e33d41`

### Mejora de arranque de sesión
- CLAUDE.md actualizado: PROHIBIDO usar mem-search al iniciar; solo leer STATE.md + diario/
- CLAUDE.md: regla explícita de actualizar STATE.md tras cualquier cambio significativo

## Modo Foco + Flujo Guiado (sesión tarde)

### Problema planteado
Usuario quería: más espacio en pantalla, menú oculto al operar, flujo lineal obligatorio (fondo→turno→operar→cerrar), cajero solo ve caja.

### Diseño aprobado (visual companion http://localhost:60329)
- Rutas operativas (`/caja`, `/farmacia`): sidebar oculto, ☰ siempre visible, overlay al abrir
- Login cajero → auto-ruta a `/caja` via ProtectedRoute role-home
- TurnoGuard bloquea acceso hasta que turno esté abierto

### Archivos creados/modificados
- `src/hooks/useSidebarState.ts` — agrega `openDrawer`
- `src/components/AppLayout.tsx` — `isFocusRoute` logic, sidebar overlay-only en rutas operativas
- `src/components/ProtectedRoute.tsx` — `ROLE_HOME` map para redirect por rol
- `src/components/TurnoGuard.tsx` (nuevo) — state machine + TurnoContext
- `src/components/turno/TurnoOpenWizard.tsx` (nuevo) — wizard apertura 3 pasos
- `src/components/turno/TurnoCloseWizard.tsx` (nuevo) — wizard cierre: conteo→diff→Corte Z
- `src/pages/Caja.tsx` — badge turno + botón cerrar vía `useTurno()`
- `src/App.tsx` — TurnoGuard envuelve `/caja` y `/farmacia`

### Desplegado
- Build: OK (solo warnings preexistentes)
- Deploy: `af1955fa` en integrika.mx

## Estado al cierre
- pharmacy shifts ahora tienen ciclo completo: open → linked a turno → closed al cerrar turno
- IVA se persiste correctamente desde próxima venta
- Responsive completo en tablet/desktop

## Pendientes que quedan
- UI para configurar umbral de diferencia (RPC ya lo lee de `clinic_settings.caja`)
- UI firma supervisor (lógica RPC existe con `p_supervisor_override`)
- CFDI / facturación real

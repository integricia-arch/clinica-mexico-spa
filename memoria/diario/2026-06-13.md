# 2026-06-13 — Stripe completo + POS farmacia

## Resumen
Sesión de cierre de ciclo Stripe y CFDI. Todo deployado y en main.

## Completado

### Stripe checkout en pitch
- Edge function `stripe-checkout` deployada (verify_jwt: false)
- Precios fijos server-side: Esencial $2,499 / Profesional $5,999 MXN/mes
- Pitch.tsx: botones "Suscribirme" → POST stripe-checkout → redirect Stripe Hosted Checkout
- Plan Empresarial conserva mailto

### Cobro Stripe en POS farmacia
- `PuntoDeVenta.tsx`: botón "Cobrar con Stripe" visible cuando método=tarjeta
- `handleStripeSuccess(paymentIntentId, txnId)` pre-llena breakdown.card
- `submitSale` refactorizado con `bdOverride` param para evitar stale closure
- `payment_transactions.sale_id` se actualiza post-venta

### Configuración producción
- `STRIPE_SECRET_KEY` + `STRIPE_WEBHOOK_SECRET` en Supabase Secrets
- `payment_gateway_config`: pk_live_ de producción, ambiente='produccion'
- Webhook Stripe → stripe-webhook edge function activo

### Deploy
- `npm run build` limpio (11s)
- `wrangler deploy` → versión `86446746`
- Push a `origin/main` (`0c2f819`)

---

# 2026-06-13 (sesión 2) — CFDI completo + fixes ALTOS diferidos

## Resumen
Cerramos todos los features CFDI pendientes y los 5 ALTOS diferidos de la sesión anterior. Módulo CFDI ahora cubre el ciclo completo: Ingreso, REP, Nota de Crédito, Cancelación con acuse receptor.

## Fixes ALTOS diferidos

### AdminUsuarios — commit 65d9a95
- `doctors` insert incluye `clinic_id: activeClinicId`
- `toggle_role` movido a edge function `admin-users` (action: toggle_role) — sync `user_roles` + `clinic_memberships` vía service_role (bypasa RLS)
- `set_base_password_all` acepta `clinic_id`, filtra por `clinic_memberships`
- Bug `handleCreate`: PIN nunca se guardaba (`data?.user?.id` → `data?.user_id`)

### restClient.ts — commit 41111a1
- Eliminado `getSupabaseConfig()` + todos `(supabase as any)` casts
- `getHeaders()` y `buildUrl()` usan `import.meta.env` directo

### CajaTurno:671 — commit c82e884
- `turno_pharmacy_link_audit` no tipado → `restSelect()` + interfaz `LinkAudit`
- Bonus: `fondos_movimientos` limpiado de `(supabase as any)`

### stripe-payment-intent — commit 9fb4cf8
- Mismatch ambiente: si key es `sk_live_` pero clínica en sandbox (o viceversa) → 500 con mensaje claro

### ConfiguracionCFDI → Vault — commit 33bd62f, deploy f6a0a897
- Migration `_tmp_cfdi_vault_secrets.sql`: RPCs `cfdi_upsert_secret` + `cfdi_get_secret` (SECURITY DEFINER, service_role only)
- Nueva edge function `cfdi-set-credentials`
- Helper `getPacCredentials()` con fallback plaintext en `cfdi-timbrar`, `cfdi-cancelar`, `cfdi-rep`

## CFDI Notas de Crédito (tipo E) — commit c3e24fc, deploy ff6912f3

### cfdi-timbrar v9
- `TimbrarRequest`: agrega `cfdi_relacionado_uuid?` + `tipo_relacion?`
- Validación: tipo E sin UUID → 400
- Payload Facturama: `CfdiRelacionados: [{TipoRelacion: "01", CfdiRelacionado: [{Uuid: ...}]}]`
- Insert BD persiste `cfdi_relacionado_uuid`

### NotaCreditoDialog.tsx (nuevo)
- Pre-carga régimen/CP/uso_cfdi desde `cfdi_receptores`
- Conceptos editables, forma_pago 17 (compensación)
- Bloque resumen del CFDI origen (folio, UUID, total)
- Submit → `cfdi-timbrar` con `tipo: "E"` + `cfdi_relacionado_uuid`

### Facturacion.tsx
- Import + state `notaCreditoDoc`
- Dropdown `FileMinus` "Nota de crédito" visible solo en CFDIs tipo I vigentes con UUID fiscal

## CFDI Acuse Receptor en Cancelación — commit f731c53, deploy 3390ac21

### cfdi-cancelar v8
- Parsea body del DELETE de Facturama
- Si `Status` contiene "request"/"pending" → BD status = `cancelacion_pendiente`
- Si 204 o status normal → `cancelado`
- Respuesta incluye `pendiente_acuse: bool`

### cfdi-acuse v1 (nueva edge function)
- `POST {clinic_id, cfdi_id}` → `GET /api/cfdis/issued/{pac_id}` en Facturama
- `parseFacturamaStatus()` normaliza a `vigente|cancelacion_pendiente|cancelado`
- Si cambió a cancelado → update BD + audit log
- Devuelve `{status, changed, message}`

### Facturacion.tsx
- `STATUS_LABEL` map: `cancelacion_pendiente` → "Cancelación pendiente"
- `STATUS_COLOR`: `cancelacion_pendiente` → `bg-warning/10 text-warning` (amarillo)
- Handler `handleVerificarAcuse`: llama cfdi-acuse, toast con mensaje, recarga tabla
- Dropdown `ClipboardCheck` "Verificar acuse receptor" visible solo en CFDIs `cancelacion_pendiente`

## Estado CFDI al cierre
- [x] Configuración emisor/PAC/CSD (con Vault)
- [x] Timbre CFDI Ingreso (tipo I)
- [x] Cancelación con 4 motivos SAT
- [x] Descarga XML/PDF
- [x] REP Complemento de Pagos (tipo P)
- [x] Factura Global (XAXX)
- [x] Nota de Crédito (tipo E) con CfdiRelacionados
- [x] Acuse receptor en cancelación

## Panel Configuración Email — commit 2384a5c, deploy 75d805b1

### Cambios
- `SettingsSection` union: agrega `"email"`
- `ConfiguracionEmail.tsx` (nuevo): campos `from_name`, `from_email`, `reply_to` vía `useClinicSettingsForm(clinicId, "email", defaults)`
- `App.tsx`: route `/configuracion/email` adminOnly
- `Configuracion.tsx`: card "Email y notificaciones" en grid de secciones, import `Mail`
- `cfdi-email` v2: lee `clinic_settings` section=`email` → construye `"${from_name} <${from_email}>"` como from Resend; agrega `reply_to` si configurado; fallback a `RESEND_FROM` env var

### Flujo
Admin → Configuración → Email y notificaciones → guarda from_name/from_email/reply_to → cfdi-email los usa automáticamente

## Pendiente próxima sesión
- Bugs conocidos: ninguno activo
- BAJOs diferidos (trade-off consciente): Farmacia forceMount, PuntoDeVenta pendiente controlados, Expedientes restSelect

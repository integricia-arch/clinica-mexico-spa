# Sesión Jun 20, 2026

## Lo que hicimos

### Plan Bot + Google Calendar — COMPLETADO ✅

Tasks 14-16 implementadas (1-13 ya estaban de sesión anterior):

**Task 14 — free/busy en listarHorariosDisponibles**
- Import `getDoctorCalendar` + `getFreeBusy` desde `google-calendar.ts`
- Pre-carga busy slots de todos los doctores en paralelo (`Promise.all`)
- Filtra slots que colisionan con Google Calendar del doctor antes de mostrarlos

**Task 15 — CRUD eventos Google Calendar**
- `crearCitaDesdeSesion`: crea evento en GCal del doctor tras confirmar cita (background)
- `confirmarCancelacionCita`: elimina evento del GCal del doctor al cancelar (background)
- `confirmarReagendar`: actualiza evento en GCal del doctor al reagendar (background)
- Todas las operaciones son fire-and-forget — no bloquean respuesta al paciente

**Task 16 — UI AdminUsuarios**
- Columna "Google Calendar" en tabla de médicos
- Muestra punto verde + email si conectado
- Botón "Conectar" → abre popup OAuth Google si `VITE_GOOGLE_CLIENT_ID` está configurado
- RPC `get_doctor_calendars(p_clinic_id)` con SECURITY DEFINER para no exponer tokens

**Configuración Google**
- Nuevo OAuth client en Google Cloud (`integrika.mx` → `Cliente web 2`)
- Authorized redirect URI: `https://kyfkvdyxpvpiacyymldc.supabase.co/functions/v1/google-oauth-callback`
- Secrets `GOOGLE_CLIENT_ID` + `GOOGLE_CLIENT_SECRET` seteados en Supabase

## Commits
- `66bf606` — feat: Google Calendar bidireccional — Tasks 14-16
- Deploy Workers: `e58cf44d`
- Push `298642c..66bf606` a origin/main

## Pendiente inmediato
- Añadir `VITE_GOOGLE_CLIENT_ID=<client_id>` a `.env` local (para que botón aparezca en dev)
- Añadir `VITE_GOOGLE_CLIENT_ID` a GitHub Actions secrets (para que aparezca en prod)
- **Probar flujo completo**: ir a `/admin/usuarios` → tab Médicos → clic "Conectar" → autorizar en Google → confirmar que columna muestra email verde → agendar cita vía bot → verificar evento en Google Calendar del doctor

## Próxima sesión — Bot + GCal completo
Plan bot+GCal terminado. Opciones siguientes:
- Limpieza de botones (Parte C del plan enfermería — 6 AlertDialogs)
- Commit pendiente de archivos enfermería (`NuevaCitaDialog`, `telegram-webhook`, `VincularTelegram`, etc.)
- Corte de caja (ver `CLAUDE.md` sección Corte de caja Opción B)

---

## Sesión 2 — Bug fixes bot Telegram (Jun 20, 2026 noche)

### Bugs corregidos ✅ (commit `e64ce37`, deploy prod)

**Bug 1: "No hay servicios disponibles en esta categoría ahora."**
- Causa: PostgREST no soporta filtros 2 niveles de anidación — `.eq("doctor_servicios.doctors.activo", true)` devuelve vacío sin error
- Fix: `getServiciosConDoctorActivo()` helper — query 1-nivel desde `doctor_servicios` JOIN `doctors`
- Usado en: `getCategoriasDisponibles()`, `enviarServiciosDeCategoria()`, `buscarServicios()`

**Bug 2: Doble "Elige la especialidad:"**
- Causa 1: `esSaludo()` con wizard activo → caía al agente LLM → llamaba `mostrar_menu_categorias`
  Fix: `esSaludo` siempre `limpiarSesion()` + `enviarMenuPrincipal()` + `return`
- Causa 2: doble-tap en botón — fix: `limpiarTeclado()` con `await` + `processedCallbackIds` Set TTL 30s

**Bug 3: Doble-booking concurrente**
- Migration: `appointments_no_double_booking` EXCLUDE USING gist (btree_gist, tstzrange overlap)
- Bot: captura 23P01 → `slotTomado: true` → ofrece siguiente slot

### Verificado en prod
- 10:58 → un solo "Elige la especialidad:" ✅
- 11:00 → "🩺 Medicina general / Elige un servicio:" ✅
- 11:01 → "Horarios para Consulta General:" ✅

### Aprendizaje
PostgREST: solo 1 nivel de filtro embedded funciona. `limpiarTeclado` debe ser `await`.

### Pendiente
- `VITE_GOOGLE_CLIENT_ID` en `.env` local + GitHub Actions secrets

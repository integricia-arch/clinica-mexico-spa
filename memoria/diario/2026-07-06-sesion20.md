# Sesión 20 — 2026-07-06

**Costo:** ~$993 USD (muy por encima de lo normal — sesión larguísima, múltiples tareas grandes encadenadas).

## Qué se hizo (ver detalle completo en [[STATE]])

1. **Investigación de costeo modular y pricing** (Integrika/ClínicaMX) — solo en chat, 2 artifacts, sin código.
2. **Fix real:** `ManualButton.tsx` mostraba el manual de Panel Principal en cualquier pantalla sin manual propio (bug de matching de prefijo `/`).
3. **Auditoría completa de 29 rutas** de la app — 21 manuales nuevos, 4 corregidos, migración `manual_paginas` aplicada.
4. **RLS Fase 1**: 264 policies / 121 tablas — `auth.uid()` envuelto en `(select ...)`. Advisor performance 264→1.
5. **`/pitch`**: comparativa de precio por doctor, animaciones, accesibilidad (5/7 de auditoría Vercel), fotos de testimonios reales, sección Tecnología reescrita en lenguaje simple, cierre de ciclo visual en "Cómo funciona", y varias vueltas en CICLO 360 (3 bugs reales de sincronización encontrados y corregidos).
6. **2 merges con Lovable** durante la sesión (trabajo en paralelo desde su dashboard) — el segundo con conflicto real, resuelto a mano.

## Pendiente real para la próxima sesión

- **Prioridad 1:** las tarjetas de módulo del CICLO 360 no se ven centradas/proporcionadas sobre la elipse — hipótesis de `cqi`/`container-type` descartada, causa real sin identificar. Necesita revisión visual en Chrome real (desktop ≥900px) antes de tocar más CSS.
- Fotos locales del usuario (`OneDrive\Pictures\hospital\pexels-gustavo-fring-4173251.jpg` y `pexels-mart-production-7088524.jpg`) para agregar a testimonios — usuario frenó con "alto!" antes de aplicar nada. Archivos pesados (5.5MB/3.8MB), hay que comprimir antes (ImageMagick `convert` disponible).
- Manual-site (Docusaurus) sin actualizar con los 21 slugs nuevos.
- 2 hallazgos menores de accesibilidad sin cerrar (preconnect de fuente, `aria-hidden` en íconos).
- `auth.email()` en `loyalty_members` — 1 finding suelto de RLS fuera del alcance de esta fase.

## Lección de la sesión

Cuando el usuario reporta "algo no se ve bien" en visual/CSS, **no hay atajo sin ver la pantalla real** — dos hipótesis seguidas sobre el CICLO 360 (el punto desincronizado, luego el centrado de tarjetas) se resolvieron/descartaron por lectura de código, pero la última quedó sin resolver porque de código no se puede confirmar qué se ve mal sin captura o navegador real. Priorizar Chrome/screenshot antes de iterar CSS a ciegas cuando el reporte es puramente visual.

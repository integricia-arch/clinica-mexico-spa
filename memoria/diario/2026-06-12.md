# 2026-06-12 — PR merge + DB local sync fix

## Resumen

Sesión corta de cierre. PR mergeado a main, schema SQL Server local actualizado.

## Completado

### PR a main
- PR #1 creado vía GitHub REST API (sin `gh` CLI) con token OAuth
- Mergeado vía squash: commit `4179e9d` en main
- `git pull origin main` — local al día

### DB local SQL Server — schema actualizado
3 tablas tenían columnas faltantes vs Supabase production:

**`medicamentos`** — agregadas 15 columnas:
`clinic_id`, `is_controlled`, `sale_type`, `requires_prescription`, `requires_retained_prescription`, `requires_special_prescription`, `allow_direct_sale`, `regulatory_notes`, `barcode`, `sku`, `codigo_interno`, `laboratorio`, `presentacion`, `registro_sanitario`, `tasa_iva`

**`lotes_medicamento`** — agregadas 3 columnas:
`clinic_id`, `fecha_entrada`, `costo_unitario`

**`movimientos_inventario`** — agregadas 3 columnas:
`clinic_id`, `reference_type`, `reference_id`

Sync post-fix: 51/51 medicamentos, 51/51 lotes, 61/61 movimientos — 0 errores, 0 warnings.

### Deploy
- `npm run build` limpio (9.3s)
- `wrangler deploy` → versión `3ca00e63` en `https://integrika.mx/`

## Sesión 2 (misma fecha)

### Bug fix: autorizado_by
- `turno_close` + `pharmacy_close_shift` + `turno_close_with_pin`: `p_supervisor_id uuid DEFAULT NULL`; `autorizado_by = COALESCE(p_supervisor_id, v_user)`
- `SupervisorAuthDialog`: password path pasa `p_supervisor_id`; pharmacy mode llama `pharmacy_close_shift`
- Migration `_tmp_fix_autorizado_by.sql` aplicada
- PR #2 mergeado (`5ef9d6c`), deploy `246bfb1f`

## Pendiente para próximas sesiones
- Bug: pharmacy PIN override en `SupervisorAuthDialog` usa `turno_close_with_pin` con `pharmacy_cash_shifts.id` — falla
- CFDI: integración PAC (largo plazo)
